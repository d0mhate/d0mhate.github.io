!function(){let n="sphere";const e="lastMode";try{n="sphere"===localStorage.getItem(e)?"matrix":"sphere",localStorage.setItem(e,n)}catch(e){n="sphere"===n?"matrix":"sphere"}if("matrix"===n)return void function(){const n=document.querySelector("canvas.three-canvas");n&&(n.style.display="none");let e=document.getElementById("matrix");e||(e=document.createElement("canvas"),e.id="matrix",document.body.appendChild(e));e.style.display="block";const t=e.getContext("2d");let a=window.innerWidth,o=window.innerHeight;const i="ABCDEFGHIJKLMNOPQRSTUVWXYZ123456789@#$%^&*()*&^%",r=18;let l=Math.floor(a/r),c=[],s=[],m={frames:0,offset:0,cols:[]},d=0;const u=4;function f(){let n=document.querySelector(".username-container");if(!n){n=document.createElement("div"),n.className="username-container";const e="d0mhate";for(let t=0;t<e.length;t++){const a=document.createElement("span");a.className="letter",a.textContent=e[t],n.appendChild(a)}document.body.appendChild(n)}n.style.display="flex"}function v(){a=window.innerWidth,o=window.innerHeight,e.width=a,e.height=o,l=Math.max(1,Math.floor(a/r)),c=[],s=[];for(let n=0;n<l;n++)c[n]=Math.floor(Math.random()*o/r),s[n]=0}function p(){t.fillStyle="rgba(0, 0, 0, 0.06)",t.fillRect(0,0,a,o),t.font=`${r}px monospace`,d++;const n=.001*performance.now(),e=.88+.12*Math.sin(.8*n);if(Math.random()<.006&&l>0){const n=Math.floor(Math.random()*l);s[n]=8+Math.floor(8*Math.random())}if(m.frames<=0&&Math.random()<.004&&l>4){m.frames=2+Math.floor(3*Math.random()),m.offset=(Math.random()>.5?1:-1)*(6+10*Math.random()),m.cols=[];const n=4+Math.floor(8*Math.random());for(let e=0;e<n;e++)m.cols.push(Math.floor(Math.random()*l))}else m.frames>0&&m.frames--;if(d%u==0)for(let n=0;n<c.length;n++){const a=s[n]>0;a&&s[n]--;const l=Math.min(255,Math.floor(140*e+80)),d=n*r,u=c[n]*r,f=i.charAt(Math.floor(Math.random()*i.length));if(t.fillStyle=`rgba(0, ${l}, 0, 0.85)`,t.fillText(f,d,u),a){const n=6;for(let e=0;e<n;e++){const n=Math.max(.08,1-.15*e),a=Math.max(60,170-18*e);t.fillStyle=`rgba(${Math.max(0,a-40)}, ${Math.min(255,a+80)}, ${Math.max(60,a)}, ${n})`,t.fillText(f,d,u-r*e)}}m.frames>0&&m.cols.includes(n)&&(t.save(),t.translate(m.offset,0),t.fillStyle="rgba(120, 255, 180, 0.3)",t.fillText(f,d,u+.3*r),t.restore()),c[n]*r>o&&Math.random()>.975&&(c[n]=0),c[n]++}requestAnimationFrame(p)}v(),f(),window.addEventListener("resize",v),p()}();const t=document.getElementById("matrix");t&&(t.style.display="none");const a=document.querySelector(".username-container");a&&(a.style.display="none");const o=new THREE.Scene;o.background=new THREE.Color(0);const i=new THREE.PerspectiveCamera(60,window.innerWidth/window.innerHeight,.1,1e3);i.position.z=230;const r=new THREE.WebGLRenderer({antialias:!0});r.setSize(window.innerWidth,window.innerHeight),r.setPixelRatio(window.devicePixelRatio),r.domElement.classList.add("three-canvas"),document.body.appendChild(r.domElement);const l=new THREE.BufferGeometry,c=8e4,s=new Float32Array(24e4),m=new Float32Array(c),d="ｱｲｳｴｵｶｷｸｹｺｻｼｽｾｿﾀﾁﾂﾃﾄﾅﾆﾇﾈﾉﾊﾋﾌﾍﾎﾏﾐﾑﾒﾓﾔﾕﾖﾗﾘﾙﾚﾛﾜﾝ0123456789",u=16,f=Math.PI*(3-Math.sqrt(5));for(let n=0;n<c;n++){const e=(n+.5)/c,t=Math.acos(1-2*e),a=f*n,o=80*Math.cos(a)*Math.sin(t),i=80*Math.sin(a)*Math.sin(t),r=80*Math.cos(t),l=3*n;s[l]=o,s[l+1]=i,s[l+2]=r,m[n]=Math.floor(55*Math.random())}l.setAttribute("position",new THREE.BufferAttribute(s,3));const v=new THREE.BufferAttribute(m,1);l.setAttribute("aChar",v);const p=function(){const n=512,e=document.createElement("canvas");e.width=e.height=n;const t=e.getContext("2d");t.fillStyle="rgba(0,0,0,0)",t.fillRect(0,0,n,n),t.fillStyle="white",t.textAlign="center",t.textBaseline="middle";const a=32;t.font="25.6px monospace";for(let n=0;n<256;n++){const e=d[n%55],o=n%u*a+16,i=Math.floor(n/u)*a+16+2.56;t.fillText(e,o,i)}const o=new THREE.CanvasTexture(e);return o.needsUpdate=!0,o.wrapS=o.wrapT=THREE.ClampToEdgeWrapping,o.minFilter=THREE.LinearFilter,o.magFilter=THREE.LinearFilter,o}(),h={uTime:{value:0},uRadius:{value:80},uNoiseAmp:{value:12},uNoiseFreq1:{value:3},uNoiseFreq2:{value:8},uViscosity:{value:.65},uFlowStrength:{value:.3},uFlowSpeed:{value:.6},uFlowToImpact:{value:0},uStreamTime:{value:-100},uStreamStrength:{value:0},uStreamPole:{value:new THREE.Vector3(0,1,0)},uImpactTime:{value:-100},uImpactStrength:{value:0},uImpactDir:{value:new THREE.Vector3(0,1,0)},uImpactType:{value:0},uMode:{value:0},uEnergy:{value:1},uIrritation:{value:0},uAtlas:{value:p},uAtlasGrid:{value:new THREE.Vector2(u,u)}},y=new THREE.ShaderMaterial({uniforms:h,vertexShader:"\n        uniform float uTime;\n        uniform float uRadius;\n        uniform float uNoiseAmp;\n        uniform float uNoiseFreq1;\n        uniform float uNoiseFreq2;\n        uniform float uViscosity;\n        uniform float uFlowStrength;\n        uniform float uFlowSpeed;\n        uniform float uFlowToImpact;\n        uniform float uStreamTime;\n        uniform float uStreamStrength;\n        uniform vec3  uStreamPole;\n        uniform float uImpactTime;\n        uniform float uImpactStrength;\n        uniform vec3  uImpactDir;\n        uniform int uMode;\n        uniform int uImpactType;\n        uniform float uEnergy;\n        uniform float uIrritation;\n        attribute float aChar;\n        varying float vChar;\n\n        varying float vNoise;\n        varying float vImpact;\n        varying float vCrest;\n\n        vec3 mod289(vec3 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }\n        vec4 mod289(vec4 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }\n        vec4 permute(vec4 x) { return mod289(((x*34.0)+1.0)*x); }\n        vec4 taylorInvSqrt(vec4 r){ return 1.79284291400159 - 0.85373472095314 * r; }\n\n        float snoise(vec3 v){\n            const vec2  C = vec2(1.0/6.0, 1.0/3.0) ;\n            const vec4  D = vec4(0.0, 0.5, 1.0, 2.0);\n\n            vec3 i  = floor(v + dot(v, C.yyy) );\n            vec3 x0 =   v - i + dot(i, C.xxx) ;\n\n            vec3 g = step(x0.yzx, x0.xyz);\n            vec3 l = 1.0 - g;\n            vec3 i1 = min( g.xyz, l.zxy );\n            vec3 i2 = max( g.xyz, l.zxy );\n\n            vec3 x1 = x0 - i1 + C.xxx;\n            vec3 x2 = x0 - i2 + C.yyy;\n            vec3 x3 = x0 - D.yyy;\n\n            i = mod289(i);\n            vec4 p = permute( permute( permute(\n                        i.z + vec4(0.0, i1.z, i2.z, 1.0 ))\n                      + i.y + vec4(0.0, i1.y, i2.y, 1.0 ))\n                      + i.x + vec4(0.0, i1.x, i2.x, 1.0 ));\n\n            float n_ = 0.142857142857;\n            vec3  ns = n_ * D.wyz - D.xzx;\n\n            vec4 j = p - 49.0 * floor(p * ns.z * ns.z);\n\n            vec4 x_ = floor(j * ns.z);\n            vec4 y_ = floor(j - 7.0 * x_ );\n\n            vec4 x = x_ *ns.x + ns.yyyy;\n            vec4 y = y_ *ns.x + ns.yyyy;\n            vec4 h = 1.0 - abs(x) - abs(y);\n\n            vec4 b0 = vec4( x.xy, y.xy );\n            vec4 b1 = vec4( x.zw, y.zw );\n\n            vec4 s0 = floor(b0)*2.0 + 1.0;\n            vec4 s1 = floor(b1)*2.0 + 1.0;\n            vec4 sh = -step(h, vec4(0.0));\n\n            vec4 a0 = b0.xzyw + s0.xzyw*sh.xxyy ;\n            vec4 a1 = b1.xzyw + s1.xzyw*sh.zzww ;\n\n            vec3 p0 = vec3(a0.xy,h.x);\n            vec3 p1 = vec3(a0.zw,h.y);\n            vec3 p2 = vec3(a1.xy,h.z);\n            vec3 p3 = vec3(a1.zw,h.w);\n\n            vec4 norm = taylorInvSqrt(vec4(\n                dot(p0,p0), dot(p1,p1), dot(p2,p2), dot(p3,p3)\n            ));\n            p0 *= norm.x;\n            p1 *= norm.y;\n            p2 *= norm.z;\n            p3 *= norm.w;\n\n            vec4 m = max(0.6 - vec4(\n                dot(x0,x0), dot(x1,x1),\n                dot(x2,x2), dot(x3,x3)\n            ), 0.0);\n            m = m * m;\n            return 42.0 * dot(m*m, vec4(\n                dot(p0,x0), dot(p1,x1),\n                dot(p2,x2), dot(p3,x3)\n            ));\n        }\n\n        // ориентировочный curl-шум для тангенциального сдвига точек\n        vec3 curlNoise(vec3 p) {\n            float e = 0.15;\n            vec3 dx = vec3(e, 0.0, 0.0);\n            vec3 dy = vec3(0.0, e, 0.0);\n            vec3 dz = vec3(0.0, 0.0, e);\n\n            float x1 = snoise(p + dy);\n            float x2 = snoise(p - dy);\n            float y1 = snoise(p + dz);\n            float y2 = snoise(p - dz);\n            float z1 = snoise(p + dx);\n            float z2 = snoise(p - dx);\n\n            float x = y1 - y2 - (z1 - z2);\n            float y = z1 - z2 - (x1 - x2);\n            float z = x1 - x2 - (y1 - y2);\n\n            return normalize(vec3(x, y, z) / (2.0 * e + 1e-5));\n        }\n\n        void main() {\n            vec3 dir = normalize(position);\n\n            // тангенциальный поток по поверхности (как течение)\n            vec3 curlP = dir * 1.8 + vec3(0.0, 0.0, uTime * uFlowSpeed);\n            vec3 curl = curlNoise(curlP);\n            // проекция на касательную плоскость\n            vec3 tangential = curl - dir * dot(curl, dir);\n            float tangentialLen = length(tangential);\n            vec3 flowDir = normalize(tangential + 1e-5);\n            float flowPulse = 0.6 + 0.4 * sin(uTime * 1.5 + tangentialLen * 4.0);\n            float flowAmount = uFlowStrength * flowPulse * (0.4 + tangentialLen);\n            // подтягиваем поток в сторону последнего удара\n            vec3 impactDir = normalize(uImpactDir);\n            float hemi = clamp(dot(impactDir, dir) * 0.5 + 0.5, 0.0, 1.0);\n            float flowToImpact = uFlowToImpact * pow(hemi, 4.0); // сильно локализуем приток\n            vec3 impactPull = normalize(impactDir - dir * dot(impactDir, dir) + 1e-5);\n            dir = normalize(dir + flowDir * flowAmount + impactPull * flowToImpact * 0.45);\n\n            // редкие «струи», стекающие с выбранного полюса вниз\n            float streamDisp = 0.0;\n            if (uStreamStrength > 0.001) {\n                float sdt = uTime - uStreamTime;\n                if (sdt >= 0.0) {\n                    float life = 9.0;\n                    float fade = exp(-sdt / life);\n                    float progress = sdt * 0.48; // скорость сползания (в радианах по сфере)\n\n                    vec3 pole = normalize(uStreamPole);\n                    vec3 up = (abs(pole.y) > 0.8) ? vec3(1.0, 0.0, 0.0) : vec3(0.0, 1.0, 0.0);\n                    vec3 right = normalize(cross(pole, up));\n                    vec3 forward = normalize(cross(right, pole));\n\n                    float ang = acos(clamp(dot(dir, pole), -1.0, 1.0));\n                    float az = atan(dot(dir, right), dot(dir, forward)); // 0 = меридиан струи\n\n                    float band = exp(-pow(ang - progress, 2.0) * 70.0);  // узкая лента по углу\n                    float thin = exp(-pow(az, 2.0) * 28.0);              // узкая по ширине\n                    float streamMask = band * thin * fade;\n\n                    // смещение вдоль меридиана «вниз» от полюса\n                    vec3 meridian = normalize(dir - pole * dot(dir, pole));\n                    dir = normalize(dir - meridian * streamMask * 0.6);\n                    streamDisp = streamMask * uStreamStrength;\n                }\n            }\n\n            // базовый мягкий шум\n            float n1 = snoise(dir * uNoiseFreq1 + vec3(0.0, 0.0, uTime * 0.5));\n            // более мелкая «текущая» детализация\n            float n2 = snoise(dir * uNoiseFreq2 + vec3(uTime * 0.8, 0.0, 0.0));\n            // волна, которая как бы «проходит» по сфере\n            float band = sin(uTime * 1.2 + dot(dir, vec3(0.0, 1.0, 0.0)) * 8.0);\n\n            // комбинируем: база + живая бегущая структура\n            float combined = n1 * 0.7 + n2 * 0.3 * band;\n            // вязкость: замедленное догоняющее смещение\n            float lagged = snoise(dir * uNoiseFreq1 + vec3(0.0, 0.0, (uTime - 0.8) * 0.35));\n            float viscousNoise = mix(combined, lagged, clamp(uViscosity, 0.0, 1.0));\n            vNoise = viscousNoise;\n            vCrest = smoothstep(0.4, 0.75, viscousNoise);\n            vChar = aChar;\n\n            // --- эффект «прокола» / импульса (жидкая деформация) ---\n            float impact = 0.0;\n            float dt = uTime - uImpactTime;\n\n            if (dt > 0.0) {\n                // сколько времени прошло с момента удара\n                float life = dt;\n\n                // частота/затухание, зависящие от режима и раздражения\n                float freq = (uMode == 1) ? 6.0 : 3.0;        // в злом режиме дрожит чаще\n                float decay = (uMode == 1) ? 1.0 : 1.2;       // чуть дольше держится\n                float irritK = clamp(uIrritation, 0.0, 1.0);\n                // раздражение делает волну острее и быстрее\n                freq *= mix(1.0, 1.8, irritK);\n                decay *= mix(1.0, 0.55, irritK);\n                // вязкость растягивает затухание и понижает частоту\n                float viscK = clamp(uViscosity, 0.0, 1.0);\n                decay *= mix(1.0, 1.85, viscK);\n                freq = mix(freq, freq * 0.7, viscK);\n                // специальные формы для редких «обвалов» и широких колец\n                if (uImpactType == 1) { // обвал\n                    freq = 5.0;\n                    decay = 2.0;\n                } else if (uImpactType == 2) { // большое кольцо\n                    freq = 2.4;\n                    decay = 1.8;\n                }\n                float temporal = exp(-life / decay) * sin(life * freq);\n\n                // угол между направлением точки и направлением «прокола»\n                float cosAngle = clamp(dot(dir, normalize(uImpactDir)), -1.0, 1.0);\n                float ang = acos(cosAngle); // 0 в центре удара\n\n                // параметры формы волны\n                float speed      = (uMode == 1) ? 3.0 : 2.0;   // скорость фронта волны\n                float centerK    = (uMode == 1) ? 18.0 : 10.0; // резкость центрального прогиба\n                float ringK      = (uMode == 1) ? 16.0 : 8.0;  // резкость кольца\n                float irritShape = clamp(uIrritation, 0.0, 1.0);\n                centerK *= mix(1.0, 3.4, irritShape); // в раздражении резче\n                ringK   *= mix(1.0, 2.6, irritShape);\n                if (uImpactType == 1) { // обвал — резче в центре\n                    speed = 2.3;\n                    centerK = 26.0;\n                    ringK = 18.0;\n                } else if (uImpactType == 2) { // большая кольцевая волна — растягиваем кольцо\n                    speed = 1.2;\n                    centerK = 6.0;\n                    ringK = 5.0;\n                }\n\n                // фронт волны, который расширяется по сфере\n                float waveFront = life * speed;\n                float diff = ang - waveFront;\n\n                // кольцевая волна вокруг точки удара\n                float ring = exp(-diff * diff * ringK);\n\n                // центральный «прогиб» вокруг точки удара\n                float center = exp(-ang * ang * centerK);\n\n                // итоговый эффект: центр + кольцо, оба с жидкой осцилляцией\n                float shapeBoost = (uImpactType == 1) ? 1.4 : ((uImpactType == 2) ? 1.15 : 1.0);\n                float irritBoost = 1.0 + irritK * 0.9;\n                impact = (center + ring * 0.8) * temporal * uImpactStrength * shapeBoost * irritBoost;\n\n                vImpact = impact;\n            } else {\n                vImpact = 0.0;\n            }\n\n            // при «проколе» сфера прогибается ВОВНУТРЬ (вычитаем impact)\n            float irritKDisp = mix(1.0, 1.4, clamp(uIrritation, 0.0, 1.0));\n            float displacement = viscousNoise * uNoiseAmp * irritKDisp - impact - streamDisp * 12.0;\n            vec3 newPos = dir * (uRadius + displacement);\n\n            vec4 mvPosition = modelViewMatrix * vec4(newPos, 1.0);\n            gl_Position = projectionMatrix * mvPosition;\n\n            float size = 3.2 + combined * 1.1 + impact * 0.6;\n            gl_PointSize = size * (220.0 / -mvPosition.z);\n        }\n    ",fragmentShader:"\n        varying float vNoise;\n        varying float vImpact;\n        uniform float uTime;\n        uniform int uMode;\n        uniform float uEnergy;\n        uniform float uIrritation;\n        varying float vCrest;\n        varying float vChar;\n        uniform sampler2D uAtlas;\n        uniform vec2 uAtlasGrid;\n\n        void main() {\n            float d = length(gl_PointCoord - vec2(0.5));\n            if (d > 0.5) discard;\n\n            float glow = smoothstep(0.5, 0.0, d);\n\n            // нормализуем шум\n            float n = vNoise * 0.5 + 0.5;\n            // лёгкий пульс по времени — чтобы точки чуть «дышали»\n            float pulse = 0.5 + 0.5 * sin(uTime * 1.3 + n * 6.0);\n\n            float intensity = 0.6\n                              + (n - 0.5) * 0.2      // небольшая разница по поверхности\n                              + (pulse - 0.5) * 0.15      // мягкий временной пульс\n                              + abs(vImpact) * 0.25;      // локальное усиление яркости от деформации\n            intensity = clamp(intensity, 0.0, 1.0);\n\n            vec3 baseColor;\n            // сдвиг цветовой температуры в зависимости от энергии: холоднее при низкой\n            float temp = clamp(uEnergy, 0.0, 1.0);\n            float coolMix = smoothstep(0.0, 0.4, 1.0 - temp);\n            float irrit = clamp(uIrritation, 0.0, 1.0);\n\n            // 0 = спокойный режим\n            if (uMode == 0) {\n                vec3 calmA = mix(vec3(0.3, 0.35, 0.8), vec3(0.8, 0.28, 0.08), 1.0 - coolMix); // тёплый/холодный\n                vec3 calmB = mix(vec3(0.5, 0.65, 1.0), vec3(1.0, 0.45, 0.15), 1.0 - coolMix);\n                baseColor = mix(calmA, calmB, intensity);\n            }\n            // 1 = злой / бурлящий\n            else if (uMode == 1) {\n                vec3 angryA = mix(vec3(0.25, 0.1, 0.6), vec3(0.55, 0.05, 0.05), 1.0 - coolMix);\n                vec3 angryB = mix(vec3(0.4, 0.25, 1.0), vec3(1.0, 0.1, 0.1), 1.0 - coolMix);\n                baseColor = mix(angryA, angryB, intensity * 1.3);\n            }\n            // 2 = хилящийся (светлый, желтоватый)\n            else {\n                vec3 healA = mix(vec3(0.6, 0.7, 1.0), vec3(1.0, 0.7, 0.25), 1.0 - coolMix);\n                vec3 healB = mix(vec3(0.8, 0.9, 1.0), vec3(1.0, 0.9, 0.6), 1.0 - coolMix);\n                baseColor = mix(healA, healB, intensity);\n            }\n\n            // раздражение добавляет красный оттенок и повышает яркость\n            vec3 angryTint = vec3(1.0, 0.12, 0.1);\n            baseColor = mix(baseColor, angryTint, clamp(irrit * 1.2, 0.0, 1.0));\n            intensity *= (1.0 + irrit * 0.4);\n\n            // доп. эмиссия на гребнях\n            float crestGlow = vCrest * 0.35;\n            // выбор символа из атласа (матрица)\n            float idx = vChar;\n            vec2 cell = vec2(mod(idx, uAtlasGrid.x), floor(idx / uAtlasGrid.x));\n            vec2 uv = (cell + gl_PointCoord) / uAtlasGrid;\n            float glyph = texture2D(uAtlas, uv).r;\n            if (glyph < 0.2) discard;\n\n            vec3 color = baseColor * glow * (1.35 + crestGlow) * glyph;\n\n            gl_FragColor = vec4(color, glow * glyph);\n        }\n    ",transparent:!0,blending:THREE.AdditiveBlending,depthWrite:!1}),x=new THREE.Points(l,y);o.add(x);const M=new THREE.Mesh(new THREE.SphereGeometry(80,32,32),new THREE.MeshBasicMaterial({visible:!1}));o.add(M);const g=new THREE.Raycaster,w=new THREE.Vector2,T=new THREE.Vector3;let E=!1,z=0;let S=0,I=1,b=0,C=-100,q=0;const F=new THREE.Clock,A={amp:12,freq1:3,freq2:8},R={...A};let H=0,P=0,N=12+10*Math.random(),D=9+10*Math.random(),B=14+12*Math.random();const K=new THREE.Vector3,V=new THREE.Vector3(0,0,230);let _=0;function G(n,e,t,a={}){const{type:o=0,energyFactor:i=.01,forceAngry:r=!1}=a;C=n,h.uImpactTime.value=n,h.uImpactStrength.value=t,h.uImpactDir.value.copy(e).normalize(),h.uImpactType.value=o,h.uFlowToImpact.value=.4,_=Math.min(1,_+.02*t),b=Math.min(1,b+.05*t);const l=t*i;I=Math.max(0,I-l),(t>18&&I>.25||r)&&(S=1,q=n)}!function n(){requestAnimationFrame(n);const e=F.getDelta(),t=F.elapsedTime;h.uTime.value=t,h.uMode.value=S,I=Math.min(1,I+.02*e),t>H&&(R.amp=10+9*Math.random(),R.freq1=2.4+2.6*Math.random(),R.freq2=6+4.5*Math.random(),H=t+3.5+4*Math.random());const a=1-Math.exp(.8*-e);A.amp+=(R.amp-A.amp)*a,A.freq1+=(R.freq1-A.freq1)*a,A.freq2+=(R.freq2-A.freq2)*a,h.uNoiseAmp.value=A.amp,h.uNoiseFreq1.value=A.freq1,h.uNoiseFreq2.value=A.freq2;const l=Math.min(.95,Math.max(.4,.55+.35*(1-I))),s=.24+.22*(1-I);if(h.uViscosity.value+=.08*(l-h.uViscosity.value),h.uFlowStrength.value+=.12*(s-h.uFlowStrength.value),h.uFlowSpeed.value=.55+.08*Math.sin(.25*t),h.uEnergy.value=I,b=Math.max(0,b-.1*e),h.uIrritation.value=b,h.uFlowToImpact.value*=Math.exp(1.8*-e),t-h.uStreamTime.value>9&&(h.uStreamStrength.value=0),t>P){const n=2e3;for(let e=0;e<n;e++){const n=Math.floor(Math.random()*c);m[n]=Math.floor(55*Math.random())}v.needsUpdate=!0,P=t+.12}if(I<.2&&2!==S&&(S=2,q=t),1===S&&t-C>10&&I>.25&&(S=0,q=t),2===S&&I>.6&&t-q>5&&(S=0,q=t),E){E=!1;G(t,T,28,{type:0,energyFactor:.02,forceAngry:I>.1})}let d,u;if(0===S?(d=6,u=.03):1===S?(d=2,u=.15):(d=8,u=.01),t-C>d&&Math.random()<u){C=t,h.uImpactTime.value=t;const n=Math.random()*Math.PI*2,e=2*Math.random()-1,a=Math.sqrt(1-e*e),o=h.uImpactDir.value;let i;if(o.set(a*Math.cos(n),a*Math.sin(n),e),0===S){i=4+4*Math.random()+(Math.random()<.25?10+15*Math.random():0)}else i=1===S?15+20*Math.random():2+2*Math.random();G(t,o,i,{type:0})}if(t>N&&t-C>2.5){const n=Math.random()*Math.PI*2,e=2*Math.random()-1,a=Math.sqrt(1-e*e);K.set(a*Math.cos(n),a*Math.sin(n),e);const o=38+18*Math.random();G(t,K,o,{type:1,energyFactor:.015,forceAngry:!0}),N=t+12+10*Math.random()}if(t>D&&t-C>2){const n=Math.random()*Math.PI*2,e=2*Math.random()-1,a=Math.sqrt(1-e*e);K.set(a*Math.cos(n),a*Math.sin(n),e);const o=24+16*Math.random();G(t,K,o,{type:2,energyFactor:.012}),D=t+9+10*Math.random()}if(t>B&&h.uStreamStrength.value<.05){const n=Math.random()*Math.PI*2,e=.25+.35*Math.random();K.set(Math.cos(n)*e,1,Math.sin(n)*e).normalize(),h.uStreamPole.value.copy(K),h.uStreamTime.value=t,h.uStreamStrength.value=.7+.5*Math.random(),B=t+14+12*Math.random()}const f=1+.03*Math.sin(.7*t);x.scale.setScalar(f);const p=.0018+.001*Math.sin(.4*t);x.rotation.y+=p,x.rotation.x+=.6*p,M.rotation.copy(x.rotation),_=Math.max(0,_-1.8*e);const y=6*_,g=2.5*Math.sin(.35*t),w=2.5*Math.sin(.42*t)*.6,z=(Math.random()-.5)*y,L=(Math.random()-.5)*y;i.position.set(V.x+g+z,V.y+w+L,V.z),i.lookAt(0,0,0),r.render(o,i)}(),r.domElement.addEventListener("click",(n=>{const e=performance.now();if(e-z<400||E)return;z=e;const t=r.domElement.getBoundingClientRect();w.x=(n.clientX-t.left)/t.width*2-1,w.y=-(n.clientY-t.top)/t.height*2+1,g.setFromCamera(w,i);const a=g.intersectObject(M);if(a.length>0){const n=a[0].point;T.copy(n).normalize(),E=!0}})),window.addEventListener("resize",(()=>{i.aspect=window.innerWidth/window.innerHeight,i.updateProjectionMatrix(),r.setSize(window.innerWidth,window.innerHeight)}))}();